
Authentication tool:
 - Use a single persistent dialog box
 - Make asynchronous
 - Store server cert in gconf, prompt user to accept it

 - Use standalone (gopenconnect) as well as from NetworkManager

Connectivity:
 - Allow use of lwip + SOCKS server instead of tun

